/** Query filters shared by Benefits Records and Benefits Overall (BenefitsRecordIndexRequest). */
export interface BenefitsFilterValues {
    month: number;
    year: number;
    search: string;
    garage_group: string;
}
